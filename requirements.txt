# Python requirements for dbt and related tools
dbt-core>=1.4.0
dbt-postgres>=1.4.0

# Optional: For advanced transformations (lower versions for compatibility)
pandas>=1.3.0,<1.4.0
numpy>=1.20.0,<1.24.0

# Note: dbt-docs is included with dbt-core, no separate package needed
# Note: dbt-utils can be installed separately if needed: pip install dbt-utils
